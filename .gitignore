# Dependencies
node_modules/

# OS / editor cruft
.DS_Store
Thumbs.db
desktop.ini
*.swp
.vscode/
.idea/

# Logs
*.log
npm-debug.log*

# Environment / secrets — never commit Supabase keys or other secrets
.env
.env.local
*.local

# Build output is regenerated from source; keep it out of version control if you
# prefer contributors to run the build. Comment the next line out if you WANT to
# ship the generated curricula in the repo (recommended for a portfolio demo).
# js/frameworks.generated.js
